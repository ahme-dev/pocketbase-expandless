{
	"name": "pocketbase-expandless",
	"version": "0.3.1",
	"description": "A library to get rid of expands in pocketbase",
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && tsc && tsc --build tsconfig.es5.json"
	},
	"license": "MIT",
	"dependencies": {
		"pocketbase": "^0.12.0"
	},
	"devDependencies": {
		"typescript": "^4.9.5"
	}
}
